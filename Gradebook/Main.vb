
Module Main
    Friend GB As New Gradebook
    Friend GBFile As String = Environment.GetEnvironmentVariable("GBLOCATION", EnvironmentVariableTarget.User)
    Friend AppData As New AppData
End Module
